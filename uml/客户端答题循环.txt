@startuml
"服务器.Model" as SM -> "客户端.Model" as CM : 新的一题的数据
alt 用户思考中
    USER -> USER : 思考
else 用户答题
    USER -> CM : 选[1]
    alt 是否重置（答错且败者食尘激活中）
        CM -> CM : 重置为未答题状态
    else 不重置
        CM ->o SM : 选[1]  
    end    

else 用户使用技能
    USER -> CM : 使用技能[1]
end
@enduml